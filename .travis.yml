sudo: required
services:
  - docker

before_install:
  - docker build -t riccardofuziercayla/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true riccardofuziercayla/docker-react npm run test